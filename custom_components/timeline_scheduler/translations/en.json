{
  "config": {
    "step": {
      "user": {
        "title": "Timeline Scheduler",
        "description": "Add Timeline Scheduler? This registers the scheduling engine, its services, and the timeline card. No configuration is required."
      }
    },
    "abort": {
      "single_instance_allowed": "Timeline Scheduler is already set up; only one instance is supported."
    }
  },
  "config_subentries": {
    "schedule": {
      "title": "Schedule",
      "entry_type": "Schedule",
      "initiate_flow": {
        "user": "Add schedule",
        "reconfigure": "Edit schedule"
      },
      "step": {
        "user": {
          "title": "Add schedule",
          "description": "Create a schedule. You'll lay out its timeline of setpoints afterward in the Timeline Scheduler card.",
          "data": {
            "name": "Name",
            "target": "Target entity",
            "apply": "Apply as",
            "enabled": "Enabled"
          },
          "data_description": {
            "target": "The entity this schedule controls — a switch/light, a climate (thermostat), or a number.",
            "apply": "How each setpoint value is sent to the target — match it to your target entity:\n\n- **Switch on/off** — a switch or light. Values are on/off.\n- **Climate temperature** — a thermostat. Each setpoint sets a temperature and/or an HVAC mode; you'll choose a turn-on mode next.\n- **Number value** — a number entity. Values are plain numbers."
          }
        },
        "on_mode": {
          "title": "Turn-on mode",
          "description": "When a setpoint sets a temperature but no explicit mode, the schedule turns the thermostat on using this mode (it's only changed when it differs from the current mode).",
          "data": {
            "on_mode": "Turn on as"
          },
          "data_description": {
            "on_mode": "The HVAC mode used to turn the device on — e.g. heat, cool, heat_cool, or auto. Pick the one your thermostat supports."
          }
        },
        "reconfigure": {
          "title": "Edit schedule",
          "description": "Change this schedule's name, target, or how its values are applied. Edit the timeline itself in the card.",
          "data": {
            "name": "Name",
            "target": "Target entity",
            "apply": "Apply as",
            "enabled": "Enabled"
          },
          "data_description": {
            "target": "The entity this schedule controls — a switch/light, a climate (thermostat), or a number.",
            "apply": "How each setpoint value is sent to the target — match it to your target entity:\n\n- **Switch on/off** — a switch or light. Values are on/off.\n- **Climate temperature** — a thermostat. Each setpoint sets a temperature and/or an HVAC mode; you'll choose a turn-on mode next.\n- **Number value** — a number entity. Values are plain numbers."
          }
        }
      },
      "abort": {
        "reconfigure_successful": "Schedule updated.",
        "entry_not_loaded": "Timeline Scheduler isn't loaded. Restart Home Assistant or re-add the integration, then try again."
      }
    }
  },
  "selector": {
    "apply": {
      "options": {
        "switch_onoff": "Switch on/off",
        "climate_temperature": "Climate temperature",
        "number_set": "Number value"
      }
    }
  }
}
